[build]
builder = "NIXPACKS"
buildCommand = "echo 'Building services...'"

[deploy]
startCommand = "echo 'Deploy each service separately'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# This file is for reference
# Deploy each service (client, server, ai_service) as separate Railway services
